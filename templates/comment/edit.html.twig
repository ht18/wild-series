{% extends 'base.html.twig' %}

{% block title %}Edit Comment
{% endblock %}

{% block body %}
	<h1>Edit Comment</h1>

	{{ include('comment/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('app_comment_index', { id: comment.id, program_id: program.id, season_id: season.id, episode_id: episode.id } ) }}">back to list</a>

	{{ include('comment/_delete_form.html.twig') }}
{% endblock %}
