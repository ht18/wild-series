{% extends 'base.html.twig' %}

{% block title %}Comment
{% endblock %}

{% block body %}
	<h1>Comment</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ comment.id }}</td>
			</tr>
			<tr>
				<th>Comment</th>
				<td>{{ comment.comment }}</td>
			</tr>
			<tr>
				<th>Rate</th>
				<td>{{ comment.rate }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_comment_index', { program_id: program.id, season_id: season.id, episode_id: episode.id } ) }}">back to list</a>

	{{ include('comment/_delete_form.html.twig') }}
{% endblock %}
